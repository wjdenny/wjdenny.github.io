image_contrast(1) %>%
image_transparent("white", fuzz = 50) %>%
image_background("black")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("white", fuzz = 51) %>%
image_background("black")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("white", fuzz = 52) %>%
image_background("black")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("white", fuzz = 53) %>%
image_background("black")
cloud <- wordcloud2(
data = tidy_vault,
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "white",
color = "random-light"
)
saveWidget(cloud, "wordcloud.html", selfcontained = FALSE)
webshot::webshot("wordcloud.html", "wordcloud.png", cliprect = "viewport", vwidth = 500, vheight = 490, delay = 10)
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("white", fuzz = 53) %>%
image_background("black")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("white", fuzz = 0) %>%
image_background("black")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("white", fuzz = 2) %>%
image_background("black")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("white", fuzz = 5) %>%
image_background("black")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("white", fuzz = 10) %>%
image_background("black")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("white", fuzz = 20) %>%
image_background("black")
cloud <- wordcloud2(
data = tidy_vault,
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "rgb(22, 22, 24)",
color = "random-light"
)
saveWidget(cloud, "wordcloud.html", selfcontained = FALSE)
webshot::webshot("wordcloud.html", "wordcloud.png", cliprect = "viewport", vwidth = 500, vheight = 490, delay = 10)
file.copy(from = "wordcloud.png", to = "/home/wjdenny/Documents/antheum/content/wordcloud.png")
file.copy(from = "wordcloud.png", to = "/home/wjdenny/Documents/antheum/content/wordcloud.png", overwrite = TRUE)
cloud <- wordcloud2(
data = tidy_vault,
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "#000",
#backgroundColor = "rgb(22, 22, 24)",
color = "random-light"
)
saveWidget(cloud, "wordcloud.html", selfcontained = FALSE)
webshot::webshot("wordcloud.html", "wordcloud.png", cliprect = "viewport", vwidth = 500, vheight = 490, delay = 10)
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("#000", fuzz = 20) %>%
image_background("black")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("#000", fuzz = 20) %>%
image_background("red")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("#000", fuzz = 20) %>%
image_background("white")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("#aaa", fuzz = 20) %>%
image_background("white")
cloud <- wordcloud2(
data = tidy_vault,
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "#aaa",
#backgroundColor = "rgb(22, 22, 24)",
color = "random-light"
)
saveWidget(cloud, "wordcloud.html", selfcontained = FALSE)
webshot::webshot("wordcloud.html", "wordcloud.png", cliprect = "viewport", vwidth = 500, vheight = 490, delay = 10)
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("#aaa", fuzz = 20) %>%
image_background("white")
cloud <- wordcloud2(
data = tidy_vault,
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "black",
#backgroundColor = "rgb(22, 22, 24)",
color = "white"
)
saveWidget(cloud, "wordcloud.html", selfcontained = FALSE)
webshot::webshot("wordcloud.html", "wordcloud.png", cliprect = "viewport", vwidth = 500, vheight = 490, delay = 10)
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("black", fuzz = 20) %>%
image_background("red")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("black", fuzz = 1) %>%
image_background("red")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("black", fuzz = 1) %>%
image_background("red")
image_read("wordcloud.png") %>%
image_contrast(1) %>%
image_transparent("black", fuzz = 1)
cloud <- wordcloud2(
data = tidy_vault,
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "black",
#backgroundColor = "rgb(22, 22, 24)",
color = c("red", "blue", "green")
)
saveWidget(cloud, "wordcloud.html", selfcontained = FALSE)
cloud <- wordcloud2(
data = tidy_vault,
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "black",
#backgroundColor = "rgb(22, 22, 24)",
color = c("red", "blue", "green")
)
saveWidget(cloud, "wordcloud.html", selfcontained = FALSE)
webshot::webshot("wordcloud.html", "wordcloud.png", cliprect = "viewport", vwidth = 500, vheight = 490, delay = 10)
tidy_vault <-
# Start with a list of Markdown files in the vault.
tibble(filePath = Sys.glob("/home/wjdenny/Documents/antheum/content/*.md")) %>%
# Read in the text of each file.
mutate(text = sapply(filePath, read_file)) %>%
# Split the text column into additional rows by line.
separate_rows(text, sep="\n") %>%
# Remove non-alpha characters.
mutate(text = gsub(x = text, pattern = "[0-9]+|[[:punct:]]|\\(.*\\)", replacement = "")) %>%
# Add the line number.
group_by(filePath) %>%
mutate(line = row_number()) %>%
ungroup() %>%
# Split the text column by words.
unnest_tokens(word, text) %>%
# Remove stop words.
anti_join(stop_words) %>%
# Count words.
count(word, sort = TRUE) %>%
wordcloud2(
data = tidy_vault,
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "black",
#backgroundColor = "rgb(22, 22, 24)",
color = c("red", "blue", "green")
)
saveWidget(cloud, "wordcloud.html", selfcontained = FALSE)
tidy_vault <-
# Start with a list of Markdown files in the vault.
tibble(filePath = Sys.glob("/home/wjdenny/Documents/antheum/content/*.md")) %>%
# Read in the text of each file.
mutate(text = sapply(filePath, read_file)) %>%
# Split the text column into additional rows by line.
separate_rows(text, sep="\n") %>%
# Remove non-alpha characters.
mutate(text = gsub(x = text, pattern = "[0-9]+|[[:punct:]]|\\(.*\\)", replacement = "")) %>%
# Add the line number.
group_by(filePath) %>%
mutate(line = row_number()) %>%
ungroup() %>%
# Split the text column by words.
unnest_tokens(word, text) %>%
# Remove stop words.
anti_join(stop_words) %>%
# Count words.
count(word, sort = TRUE) %>%
wordcloud2(
data = tidy_vault,
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "black",
#backgroundColor = "rgb(22, 22, 24)",
color = c("red", "blue", "green")
)
saveWidget(cloud, "wordcloud.html", selfcontained = FALSE)
tidy_vault <-
# Start with a list of Markdown files in the vault.
tibble(filePath = Sys.glob("/home/wjdenny/Documents/antheum/content/*.md")) %>%
# Read in the text of each file.
mutate(text = sapply(filePath, read_file)) %>%
# Split the text column into additional rows by line.
separate_rows(text, sep="\n") %>%
# Remove non-alpha characters.
mutate(text = gsub(x = text, pattern = "[0-9]+|[[:punct:]]|\\(.*\\)", replacement = "")) %>%
# Add the line number.
group_by(filePath) %>%
mutate(line = row_number()) %>%
ungroup() %>%
# Split the text column by words.
unnest_tokens(word, text) %>%
# Remove stop words.
anti_join(stop_words) %>%
# Count words.
count(word, sort = TRUE) %>%
wordcloud2(
data = tidy_vault,
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "black",
backgroundColor = "rgb(22, 22, 24)",
)
tidy_vault <-
# Start with a list of Markdown files in the vault.
tibble(filePath = Sys.glob("/home/wjdenny/Documents/antheum/content/*.md")) %>%
# Read in the text of each file.
mutate(text = sapply(filePath, read_file)) %>%
# Split the text column into additional rows by line.
separate_rows(text, sep="\n") %>%
# Remove non-alpha characters.
mutate(text = gsub(x = text, pattern = "[0-9]+|[[:punct:]]|\\(.*\\)", replacement = "")) %>%
# Add the line number.
group_by(filePath) %>%
mutate(line = row_number()) %>%
ungroup() %>%
# Split the text column by words.
unnest_tokens(word, text) %>%
# Remove stop words.
anti_join(stop_words) %>%
# Count words.
count(word, sort = TRUE) %>%
wordcloud2(
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "black",
backgroundColor = "rgb(22, 22, 24)",
)
tidy_vault <-
# Start with a list of Markdown files in the vault.
tibble(filePath = Sys.glob("/home/wjdenny/Documents/antheum/content/*.md")) %>%
# Read in the text of each file.
mutate(text = sapply(filePath, read_file)) %>%
# Split the text column into additional rows by line.
separate_rows(text, sep="\n") %>%
# Remove non-alpha characters.
mutate(text = gsub(x = text, pattern = "[0-9]+|[[:punct:]]|\\(.*\\)", replacement = "")) %>%
# Add the line number.
group_by(filePath) %>%
mutate(line = row_number()) %>%
ungroup() %>%
# Split the text column by words.
unnest_tokens(word, text) %>%
# Remove stop words.
anti_join(stop_words) %>%
# Count words.
count(word, sort = TRUE) %>%
wordcloud2(
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "rgb(22, 22, 24)"
)
tidy_vault <-
# Start with a list of Markdown files in the vault.
tibble(filePath = Sys.glob("/home/wjdenny/Documents/antheum/content/*.md")) %>%
# Read in the text of each file.
mutate(text = sapply(filePath, read_file)) %>%
# Split the text column into additional rows by line.
separate_rows(text, sep="\n") %>%
# Remove non-alpha characters.
mutate(text = gsub(x = text, pattern = "[0-9]+|[[:punct:]]|\\(.*\\)", replacement = "")) %>%
# Add the line number.
group_by(filePath) %>%
mutate(line = row_number()) %>%
ungroup() %>%
# Split the text column by words.
unnest_tokens(word, text) %>%
# Remove stop words.
anti_join(stop_words) %>%
# Count words.
count(word, sort = TRUE) %>%
wordcloud2(
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "rgb(22, 22, 24)"
)
cloud
tidy_vault
cloud <-
# Start with a list of Markdown files in the vault.
tibble(filePath = Sys.glob("/home/wjdenny/Documents/antheum/content/*.md")) %>%
# Read in the text of each file.
mutate(text = sapply(filePath, read_file)) %>%
# Split the text column into additional rows by line.
separate_rows(text, sep="\n") %>%
# Remove non-alpha characters.
mutate(text = gsub(x = text, pattern = "[0-9]+|[[:punct:]]|\\(.*\\)", replacement = "")) %>%
# Add the line number.
group_by(filePath) %>%
mutate(line = row_number()) %>%
ungroup() %>%
# Split the text column by words.
unnest_tokens(word, text) %>%
# Remove stop words.
anti_join(stop_words) %>%
# Count words.
count(word, sort = TRUE) %>%
wordcloud2(
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "rgb(22, 22, 24)"
)
cloud
cloud <-
# Start with a list of Markdown files in the vault.
tibble(filePath = Sys.glob("/home/wjdenny/Documents/antheum/content/*.md")) %>%
# Read in the text of each file.
mutate(text = sapply(filePath, read_file)) %>%
# Split the text column into additional rows by line.
separate_rows(text, sep="\n") %>%
# Remove non-alpha characters.
mutate(text = gsub(x = text, pattern = "[0-9]+|[[:punct:]]|\\(.*\\)", replacement = "")) %>%
# Add the line number.
group_by(filePath) %>%
mutate(line = row_number()) %>%
ungroup() %>%
# Split the text column by words.
unnest_tokens(word, text) %>%
# Remove stop words.
anti_join(stop_words) %>%
# Count words.
count(word, sort = TRUE) %>%
wordcloud2(
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "rgb(22, 22, 24)",
color = "random-light"
)
words <-
# Start with a list of Markdown files in the vault.
tibble(filePath = Sys.glob("/home/wjdenny/Documents/antheum/content/*.md")) %>%
# Read in the text of each file.
mutate(text = sapply(filePath, read_file)) %>%
# Split the text column into additional rows by line.
separate_rows(text, sep="\n") %>%
# Remove non-alpha characters.
mutate(text = gsub(x = text, pattern = "[0-9]+|[[:punct:]]|\\(.*\\)", replacement = "")) %>%
# Add the line number.
group_by(filePath) %>%
mutate(line = row_number()) %>%
ungroup() %>%
# Split the text column by words.
unnest_tokens(word, text) %>%
# Remove stop words.
anti_join(stop_words) %>%
# Count words.
count(word, sort = TRUE)
cloudLight <- words %>%
wordcloud2(
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "rgb(22, 22, 24)",
color = "random-light"
)
cloudLight
cloudLight <- words %>%
wordcloud2(
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(500, 500),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "rgb(250, 248, 248)",
color = "random-dark"
)
cloudLight
cloudLight <- words %>%
wordcloud2(
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(750, 200),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "rgb(250, 248, 248)",
color = "random-dark"
)
cloudLight
cloudDark <- words %>%
wordcloud2(
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(750, 200),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "rgb(22, 22, 24)",
color = "random-light"
)
cloudLight <- words %>%
wordcloud2(
shape = "square",
size = 1.9,
fontWeight = "normal",
widgetsize = c(750, 200),
minRotation = -pi/16,
maxRotation = -pi/16,
rotateRatio = 1,
backgroundColor = "rgb(250, 248, 248)",
color = "random-dark"
)
saveWidget(cloudDark, "wordcloud-dark.html", selfcontained = FALSE)
webshot::webshot("wordcloud-dark.html", "wordcloud-dark.png", cliprect = "viewport", delay = 10)
webshot::webshot("wordcloud-dark.html", "wordcloud-dark.png", cliprect = "viewport", vwidth = 750, vheight = 200, delay = 10)
saveWidget(cloudLight, "wordcloud-light", selfcontained = FALSE)
webshot::webshot("wordcloud-light.html", "wordcloud-light.png", cliprect = "viewport", vwidth = 750, vheight = 200, delay = 10)
